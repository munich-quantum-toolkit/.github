version: 2

formats:
  - htmlzip

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  apt_packages:
    - imagemagick
    - graphviz
    - inkscape

python:
  install:
    - method: uv
      command: pip
      requirements: docs/requirements.txt
